#!/bin/bash
# Hook pre-push pour CryptoSpreadEdge

echo "🚀 Vérification pre-push..."

# Vérifier que les tests passent
echo "🧪 Exécution des tests..."
python -m pytest tests/ -v --tb=short
if [ $? -ne 0 ]; then
    echo "❌ Les tests ont échoué - Push annulé"
    exit 1
fi

# Vérifier la couverture de code
echo "📊 Vérification de la couverture de code..."
python -m pytest tests/ --cov=src --cov-report=term-missing --cov-fail-under=70
if [ $? -ne 0 ]; then
    echo "⚠️  Couverture de code insuffisante - Push autorisé mais attention"
fi

# Vérifier la qualité du code
echo "🔍 Vérification de la qualité du code..."
python -m black --check src/
if [ $? -ne 0 ]; then
    echo "⚠️  Code non formaté avec Black - Push autorisé mais attention"
fi

python -m isort --check-only src/
if [ $? -ne 0 ]; then
    echo "⚠️  Imports non triés avec isort - Push autorisé mais attention"
fi

# Vérifier les types
echo "🔍 Vérification des types..."
python -m mypy src/ --ignore-missing-imports
if [ $? -ne 0 ]; then
    echo "⚠️  Erreurs de type détectées - Push autorisé mais attention"
fi

# Vérifier les secrets
echo "🔐 Vérification des secrets..."
if git diff --cached --name-only | grep -q "\.env$"; then
    echo "⚠️  Fichier .env détecté dans le commit"
    echo "💡 Assurez-vous qu'aucun secret n'est exposé"
fi

# Vérifier les fichiers volumineux
echo "📏 Vérification des fichiers volumineux..."
large_files=$(git ls-files | xargs ls -la | awk '$5 > 1048576 {print $9 " (" $5 " bytes)"}')
if [ -n "$large_files" ]; then
    echo "⚠️  Fichiers volumineux détectés:"
    echo "$large_files"
fi

# Vérifier la branche
branch=$(git branch --show-current)
if [[ $branch == "master" ]] || [[ $branch == "main" ]]; then
    echo "⚠️  Push vers la branche principale détecté"
    echo "💡 Assurez-vous que c'est intentionnel"
fi

echo "✅ Vérifications pre-push terminées"